var thisp = this.patcher;var pathsA = new Array();var maxpaths = 50;function fillpaths(max){	clear();	for(i=0;i<max;i++)		pathsA[i] = thisp.newdefault(Math.random()*300,Math.random()*400,"filepath","search");}function paths(n){	if(pathsA.length < n)		for(i=pathsA.length;i<n;i++)			pathsA[i] = thisp.newdefault(Math.random()*300,Math.random()*400,"filepath","search");			}function set(){	if(arguments.length>1 && arguments[0] < pathsA.length)	{		pathsA[arguments[0]].message("set",arguments[1]);	}		}function clear(){	for(i=0;i<pathsA.length;i++)		thisp.remove(pathsA[i]);	pathsA.length = 0;}